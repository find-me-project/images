export enum ImageClassEnum {
  lowResolution = 'lowResolution',
  normalResolution = 'normalResolution',
  highResolution = 'highResolution',
}

export enum ImageClassResolutionEnum {
  lowResolution = '40',
  normalResolution = '80',
  highResolution = '100',
}
